[package]
name = "growl_core"
version = "0.1.0"
edition = "2024"
description = "Core library for validating structured Markdown wikis"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
walkdir = "2"
